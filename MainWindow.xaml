<Window x:Class="TimeWise.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:TimeWise.Controls"
        Title="TimeWise" 
        MinHeight="600" 
        MinWidth="1000"
        Height="800" 
        Width="1500"
        WindowState="Maximized"
        Background="{DynamicResource WindowBackgroundBrush}">
    <Window.Resources>
        <!-- Simple Icon Resources using Unicode symbols -->
        <Style x:Key="IconButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextForegroundBrush}"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource ButtonHoverBrush}"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{DynamicResource TextForegroundBrush}"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource CalendarTodayBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern Delete Button Style -->
        <Style x:Key="CircularDeleteButton" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="15">
                            <Border.Effect>
                                <DropShadowEffect Color="Black" BlurRadius="4" Opacity="0.3" ShadowDepth="2"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#FF8B0000" Offset="0"/>
                                            <GradientStop Color="#FFDC143C" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="1.1" ScaleY="1.1"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" 
                                               To="1.1" Duration="0:0:0.1"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" 
                                               To="1.1" Duration="0:0:0.1"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" 
                                               To="1.0" Duration="0:0:0.1"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" 
                                               To="1.0" Duration="0:0:0.1"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Modern Add Button Style -->
        <Style x:Key="CircularAddButton" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="35">
                            <Border.Effect>
                                <DropShadowEffect Color="#FF4169E1" BlurRadius="8" Opacity="0.6" ShadowDepth="3"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#FF32CD32" Offset="0"/>
                                            <GradientStop Color="#FF228B22" Offset="0.5"/>
                                            <GradientStop Color="#FF006400" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#FF32CD32" BlurRadius="12" Opacity="0.8" ShadowDepth="4"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.9" ScaleY="0.9"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" 
                                               To="1.15" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" 
                                               To="1.15" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)" 
                                               To="90" Duration="0:0:0.3"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" 
                                               To="1.0" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" 
                                               To="1.0" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)" 
                                               To="0" Duration="0:0:0.3"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Enhanced Card Style with theme-aware shadows -->
        <Style x:Key="AppointmentCard" TargetType="Border">
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="{Binding Source={x:Static SystemParameters.HighContrast}, Converter={x:Null}}" 
                                    BlurRadius="6" Opacity="0.3" ShadowDepth="3"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="{Binding Source={x:Static SystemParameters.HighContrast}, Converter={x:Null}}" 
                                            BlurRadius="10" Opacity="0.5" ShadowDepth="5"/>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="RenderTransform">
                        <Setter.Value>
                            <TranslateTransform Y="-2"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Theme-aware TextBox Style -->
        <Style x:Key="ThemedTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource TextBoxBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource TextBoxBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="3"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{DynamicResource TextBoxFocusBorderBrush}"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" MinWidth="200" MaxWidth="350"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*" MinWidth="500"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" MinHeight="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Splitter for resizable sidebar -->
        <GridSplitter Grid.Column="1" Grid.Row="0" Grid.RowSpan="2"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"
                      Background="{DynamicResource BorderBrush}"
                      ShowsPreview="True"/>

        <Grid x:Name="Header" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Label Grid.Column="0"
                   Content="TimeWise" 
                   FontSize="36" 
                   FontWeight="Bold"
                   Foreground="{DynamicResource HeaderForegroundBrush}" 
                   Height="63" 
                   VerticalContentAlignment="Center"
                   HorizontalContentAlignment="Left"
                   Margin="10,0,0,0"/>

            <!-- Buttons aligned to the right -->
            <StackPanel Grid.Column="1" 
                       Orientation="Horizontal" 
                       HorizontalAlignment="Right" 
                       VerticalAlignment="Center"
                       Margin="0,0,20,0">
                <Button x:Name="Settings" 
                        Width="50" 
                        Height="40" 
                        Margin="10,0,0,0" 
                        HorizontalAlignment="Center" 
                        VerticalAlignment="Center" 
                        Style="{StaticResource IconButtonStyle}"
                        Content="SET"
                        FontSize="16"
                        FontWeight="Bold"
                        ToolTip="Settings"/>

                <Button x:Name="Darkmode"
                        Width="56" 
                        Height="41" 
                        Margin="10,0,0,0" 
                        HorizontalAlignment="Center" 
                        VerticalAlignment="Center" 
                        Style="{StaticResource IconButtonStyle}"
                        Content="DARK"
                        FontSize="16"
                        FontWeight="Bold"
                        Click="Darkmode_Click"
                        ToolTip="Switch Theme"/>
            </StackPanel>
        </Grid>


        <Grid x:Name="SidePanel" Grid.Column="0" Grid.Row="1" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <local:SimpleCalendar x:Name="MainCalendar" 
                      Margin="5" 
                      Grid.Row="0"
                      HorizontalAlignment="Stretch"/>

            <StackPanel Grid.Row="1" Orientation="Vertical" Margin="5">
                <Border BorderBrush="{DynamicResource BorderBrush}" 
                        BorderThickness="1" 
                        Margin="5" 
                        Background="{DynamicResource PanelBackgroundBrush}"/>

                <Label Content="Category Filters" 
                       FontSize="16" 
                       FontWeight="Bold" 
                       HorizontalAlignment="Center" 
                       Margin="0,5,0,5"
                       Foreground="{DynamicResource TextForegroundBrush}"/>

                <CheckBox Content="Work" Margin="10,3,0,3" 
                         Style="{DynamicResource DarkCheckBoxStyle}"/>
                <CheckBox Content="Personal" Margin="10,3,0,3" 
                         Style="{DynamicResource DarkCheckBoxStyle}"/>
                <CheckBox Content="Health" Margin="10,3,0,3" 
                         Style="{DynamicResource DarkCheckBoxStyle}"/>
                <CheckBox Content="Hobbies" Margin="10,3,0,3" 
                         Style="{DynamicResource DarkCheckBoxStyle}"/>

                <TextBox x:Name="SearchBox" 
                         Margin="10,10,10,5" 
                         Height="25" 
                         Text="Search appointments..." 
                         HorizontalAlignment="Stretch"
                         HorizontalContentAlignment="Center"
                         Style="{StaticResource ThemedTextBox}"/>
            </StackPanel>

            <ScrollViewer Grid.Row="2" 
                         VerticalScrollBarVisibility="Auto" 
                         HorizontalScrollBarVisibility="Disabled"
                         Margin="5">
                <StackPanel>
                    <Border BorderBrush="{DynamicResource BorderBrush}" 
                            BorderThickness="1" 
                            Margin="5" 
                            Background="{DynamicResource PanelBackgroundBrush}"/>

                    <Label Content="Notes" 
                           FontSize="16" 
                           FontWeight="Bold" 
                           HorizontalAlignment="Center" 
                           Margin="0,5,0,5"
                           Foreground="{DynamicResource TextForegroundBrush}"/>

                    <TextBox x:Name="NotesBox" 
                             Margin="10,5,10,5" 
                             Height="150"
                             MinHeight="100"
                             MaxHeight="300"
                             TextWrapping="Wrap" 
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             Style="{StaticResource ThemedTextBox}"/>

                    <Border BorderBrush="{DynamicResource BorderBrush}" 
                            BorderThickness="1" 
                            Margin="10,5,10,0" 
                            Background="{DynamicResource PanelBackgroundBrush}"/>

                    <TextBlock Text="Notes will be saved automatically." 
                               HorizontalAlignment="Center" 
                               Margin="5"
                               TextWrapping="Wrap"
                               FontSize="12"
                               Foreground="{DynamicResource SecondaryTextBrush}"/>
                </StackPanel>
            </ScrollViewer>
        </Grid>

        <Grid Grid.Row="1" Grid.Column="2" Margin="10,0,10,0">
            <TabControl x:Name="MainTabControl" 
                       Background="{DynamicResource TabBackgroundBrush}"
                       Style="{DynamicResource DarkTabControlStyle}">
                <TabItem Height="30" Width="80" 
                        Style="{DynamicResource DarkTabItemStyle}">
                    <TabItem.Header>
                        <TextBlock Text="Day" Foreground="{DynamicResource TextForegroundBrush}" FontWeight="Bold"/>
                    </TabItem.Header>
                    <DockPanel>
                        <!-- Add Button first to reserve space at bottom -->
                        <Button DockPanel.Dock="Bottom" Content="+" FontSize="32" FontWeight="Bold" 
                                Width="70" Height="70" 
                                HorizontalAlignment="Center" VerticalAlignment="Center" 
                                Margin="15" Foreground="White" 
                                BorderBrush="Green" Style="{StaticResource CircularAddButton}"
                                Click="AddAppointment_Click">
                            <Button.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#FF00CED1" Offset="0"/>
                                    <GradientStop Color="#FF4169E1" Offset="0.5"/>
                                    <GradientStop Color="#FF0000FF" Offset="1"/>
                                </LinearGradientBrush>
                            </Button.Background>
                            <Button.RenderTransform>
                                <TransformGroup>
                                    <ScaleTransform/>
                                    <RotateTransform/>
                                </TransformGroup>
                            </Button.RenderTransform>
                        </Button>

                        <!-- ScrollViewer takes remaining space -->
                        <ScrollViewer VerticalScrollBarVisibility="Auto" 
                                    HorizontalScrollBarVisibility="Auto"
                                    CanContentScroll="True"
                                    Style="{DynamicResource ThemedScrollViewer}">
                            <StackPanel x:Name="appointmentList" Margin="10,10,10,10">
                                <!-- Daily appointments will be loaded here dynamically -->
                            </StackPanel>
                        </ScrollViewer>
                    </DockPanel>
                </TabItem>

                <TabItem Height="30" Width="80" VerticalAlignment="Center" 
                        Style="{DynamicResource DarkTabItemStyle}">
                    <TabItem.Header>
                        <TextBlock Text="Week" Foreground="{DynamicResource TextForegroundBrush}" FontWeight="Bold"/>
                    </TabItem.Header>
                    <DockPanel>
                        <!-- Add Button first to reserve space at bottom -->
                        <Button DockPanel.Dock="Bottom" Content="+" FontSize="32" FontWeight="Bold" 
                                Width="70" Height="70" 
                                HorizontalAlignment="Center" VerticalAlignment="Center" 
                                Margin="15" Foreground="White" 
                                BorderBrush="Green" Style="{StaticResource CircularAddButton}"
                                Click="AddAppointment_Click">
                            <Button.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#FF00CED1" Offset="0"/>
                                    <GradientStop Color="#FF4169E1" Offset="0.5"/>
                                    <GradientStop Color="#FF0000FF" Offset="1"/>
                                </LinearGradientBrush>
                            </Button.Background>
                            <Button.RenderTransform>
                                <TransformGroup>
                                    <ScaleTransform/>
                                    <RotateTransform/>
                                </TransformGroup>
                            </Button.RenderTransform>
                        </Button>

                        <!-- ScrollViewer takes remaining space -->
                        <ScrollViewer VerticalScrollBarVisibility="Auto" 
                                    HorizontalScrollBarVisibility="Auto"
                                    CanContentScroll="True"
                                    Style="{DynamicResource ThemedScrollViewer}">
                            <StackPanel x:Name="weekAppointmentList" Margin="10,10,10,10">
                                <!-- Weekly appointments will be loaded here dynamically -->
                            </StackPanel>
                        </ScrollViewer>
                    </DockPanel>
                </TabItem>
            </TabControl>
        </Grid>
    </Grid>
</Window>
